import React from "react";

import "./Posts.css";

/**
 * The navigation bar at the top of all pages. Takes no props.
 */
const Posts = ({ userId, handleLogin, handleLogout }) => {
  return <div className="Metrics-container">Hello</div>;
};

export default Posts;
