import React from "react";

import "./TextChoose.css";

/**
 * The navigation bar at the top of all pages. Takes no props.
 */
const TextChoose = ({ userId, handleLogin, handleLogout }) => {
  return <div className="TextChoose-container">Hello</div>;
};

export default TextChoose;
