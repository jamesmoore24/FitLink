import React from "react";

import "./Metrics.css";

/**
 * The navigation bar at the top of all pages. Takes no props.
 */
const Metrics = ({ userId, handleLogin, handleLogout }) => {
  return <div className="metrics-container">Hello</div>;
};

export default Metrics;
