name: Deploy FitLink

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "18"

      - name: Install dependencies
        run: npm ci

      - name: Build Frontend for Production
        run: npm run build:prod

      - name: Create .env file
        run: |
          echo "MONGO_SRV=${{ secrets.MONGO_SRV }}" > .env
          echo "SESSION_SECRET=${{ secrets.SESSION_SECRET }}" >> .env
          echo "OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}" >> .env

      - name: Install SSH key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          known_hosts: ${{ secrets.KNOWN_HOSTS }}

      - name: Deploy Backend to Server
        run: |
          rsync -avz -e "ssh -o StrictHostKeyChecking=no" \
            --delete server/ root@fitlink.live:/var/www/fitlink/backend/
          rsync -avz -e "ssh -o StrictHostKeyChecking=no" \
            --delete package.json package-lock.json .env \
            root@fitlink.live:/var/www/fitlink/backend/

      - name: Deploy Frontend to Server
        run: |
          rsync -avz -e "ssh -o StrictHostKeyChecking=no" \
            --delete client/dist/ root@fitlink.live:/var/www/fitlink/frontend/
          rsync -avz -e "ssh -o StrictHostKeyChecking=no" \
            --delete client/src/.gitkeep \
            root@fitlink.live:/var/www/fitlink/frontend/src/.gitkeep

      - name: Run post-deployment commands
        run: |
          ssh -o StrictHostKeyChecking=no root@fitlink.live '
            set -e

            # Backend setup
            echo "Setting up backend..."
            cd /var/www/fitlink/backend
            npm install --production

            # Restart backend service with PM2
            echo "Restarting backend service..."
            pm2 restart fitlink-server || pm2 start npm --name "fitlink-server" -- run start

            # Reload Nginx
            # Go
            echo "Reloading Nginx..."
            sudo systemctl reload nginx

            echo "Deployment completed successfully"
          '

      - name: Cleanup
        if: always()
        run: rm -rf ~/.ssh
